# T4 任务书:投稿与汇报策略

> **状态:DRAFT(2026-07-06)——需注入前序任务锚点后再最终分发。**
> 分发前 planner 必须:(1) 用 T3 验收结论替换「已确认锚点」中的全部 `<待注入 ...>` 占位(红线、必引、竞品清单);(2) 核实 `research/T3_novelty_adjudication.md` 已存在且已验收;(3) 与用户确认目标里程碑(内部汇报日期、期望投稿窗口)后填入「项目背景」;(4) 删除本 banner 后再交用户转发。

- 生成:2026-07-06,planner agent
- 引用路径核实(2026-07-06):`research/T0_project_analysis.md` 存在;T3 报告文件**尚不存在,分发前必须核实**

---

## 任务书正文(定稿后从此行以下全部粘贴)

你是一个强力 research agent,本次任务是 **T4 调研:投稿与汇报策略(venue 时间线、口味匹配、证据缺口分级、时间表)**。这是纯调研任务,不是工程任务。

工作目录:

/Users/chenhingchin/Desktop/ChenQingzhan-DMD-distillation

## 角色与边界(先读这一节)

- 本目录的 CLAUDE.md 是为工程 agent 写的,本次会话**忽略其中的角色设定和上下文加载规则**。
- 不要读代码目录,不要 ssh 任何远端机器,不要跑训练/评估,不要修改 `research/` 以外的任何文件。
- 本地**只读以下 2 个文件**,其余背景本 prompt 已给足,不要再读别的:
  1. `research/T0_project_analysis.md`(第 2 节证据分级、第 3 节 novelty 轴)
  2. `research/T3_novelty_adjudication.md`(红线、必引清单、竞品状态)
- **所有会议/期刊日期必须实时检索官方来源**(官网 CFP 页,不是第三方 deadline 聚合站;聚合站只能当线索),每个日期附官方链接与时区(AoE 等);查不到官方确认的标"待核实"。
- 可并行的检索尽量并行。

## 项目背景(一段话)

我们在 FastGen 框架上把 Wan2.1-T2V-1.3B 的 50-step teacher 用 DMD2 目标分阶段蒸馏为 8-step、再到 4-step student,方向定位与 novelty 红线已由 T0-T3 确定(见锚点)。本 T4 回答"往哪投、什么时候投、还差什么证据":候选 venue 的确切时间线、各 venue 口味与 framing、证据缺口分级与补齐计划、Plan A/B 时间表。目标里程碑:`<待注入:内部汇报日期与期望投稿窗口,由用户确认>`。今天是 `<分发当日日期,planner 填写>`。

## 我们的方法与证据现状(一段话,来自 T0;详情不必展开)

方法:progressive few-step distillation——50-step 文生视频 teacher(Wan2.1-T2V-1.3B)经 DMD2(distribution matching + GAN)蒸馏为 8-step 中间 student,再以其最优 checkpoint 初始化 4-step student。证据现状(诚实版):训练/推理闭环与各 run 超参已核实;速度约 25x(内部记录);**全部质量结论为肉眼判断,无量化指标**;两轮 4-from-8 之间存在 LR/batch/GPU 三因素混淆,归因主张需补受控实验;`t_list` 消融有两个已跑 run 但结论未整理。

## 已确认锚点

来自 T3(**待注入**):

- `<待注入:三条 novelty 轴终裁与幸存的窄带主张>`
- `<待注入:"能说/不能说"红线清单>`
- `<待注入:必引划界清单>`
- `<待注入:Wan 生态竞品清单与状态(含核实日期)>`
- `<待注入:T3 推荐的最小评估协议>`

来自 T0(已确认):证据缺口基线——无量化指标(最大缺口)、归因混淆、单模型单数据集、肉眼 checkpoint 选择。

## T4 要回答的问题

1. **候选 venue 确切时间线**:先核实下列候选,再补充你认为合适的 2-3 个——ICLR 2027、CVPR 2027、AAAI 2027、WACV 2027、NeurIPS 2026(确认是否已过截稿)、ACM MM 2027、ICME 2027;另给 2-3 个对口 workshop 兜底(视频生成/高效生成方向)。每个:摘要与全文截稿(含时区)、rebuttal 窗口、notification、官方链接。
2. **口味与 framing**:对排名前 3 的候选 venue,判断"蒸馏/加速配方 + 系统实证"类论文的接受口味,各给 1-2 个 framing 候选(方法贡献型 vs 实证/配方型 vs 协议型),并各附 2-3 篇近两年已录用参照(链接)。
3. **证据标准与缺口分级**:对照锚点中的证据现状,按"必须补 / 建议补 / 可不补"三档列清单;每个"必须补"给最小实验规格(例如:量化协议跑哪几个指标、覆盖哪些 checkpoint;归因实验最少几组受控 run)。注意:你只设计规格,不估算我们集群的可行性,那是 planner 与用户的事。
4. **时间表**:以首选 venue 截稿倒推 Plan A 里程碑(证据补齐 -> 写作 -> 内审 -> 投稿),并给 Plan B(次选 venue / workshop)及触发切换的判据。
5. **竞品复核点**:基于 T3 竞品清单,列出投稿前必须再核实的条目(状态可能变化:arXiv 更新、录用、新 release)与建议复核时间点。
6. 若首选 venue 时间不可行(证据补不完):给"先 workshop / 技术报告占位,再主会"路径的利弊判断。

## 产出

写入文件:`research/T4_submission_strategy.md`(覆盖写,文件名固定)

结构(六段式适配本任务):
1. **Executive Summary**:5-8 条,先给首选 venue、截稿日、最重的"必须补"证据。
2. **Venue 时间线表**:Venue / 截稿(时区)/ rebuttal / notification / 官方链接 / 核实日期。
3. **口味与 framing 卡**(前 3 venue 各一卡):口味判断、framing 候选、已录用参照(链接)。
4. **证据缺口分级表**:缺口 / 档位(必须补・建议补・可不补)/ 最小规格 / 依赖(是否依赖新实验)。
5. **Plan A / Plan B 时间表**:里程碑倒推 + 切换判据。
6. **竞品复核点与汇报表达**:复核清单 + 给内部汇报用的 3-5 条"定位+证据现状"诚实表述。

格式要求:每个日期与录用参照给链接;查不到官方确认的一律标"待核实";每节结论先行;中文撰写,会议名与术语保留英文。

## 调研纪律

- 日期零容忍:官方来源、附链接、注时区、注核实日期;聚合站信息必须回到官网确认。
- 口味判断要给证据(已录用参照),不许凭印象。
- 缺口分级要与 T3 红线一致:被判"不能说"的主张不得出现在任何 framing 里。
- 产出服务投稿决策,语气专业、清楚、克制;诚实呈现 pending 与风险。
- 完成后在对话里给 ≤10 行执行总结,必须包含:首选 venue 与截稿日;"必须补"证据条数与最重一项;Plan B 一句话;竞品复核时间点。
